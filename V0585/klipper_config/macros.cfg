#####################################################################
#   Macros
#####################################################################

[include macro_overrides.cfg]

[gcode_macro PRINT_START]
gcode:
    G28 Z                          ; always home z (heat soak can vary offset, we want a new reading)
    C_G28                          ; conditionally home X/Y, no need to home if they are already
    G90                            ; absolute positioning    
    SET_LED LED="nozzle_light" RED=0.8 GREEN=0.8 BLUE=0.8 SYNC=0 TRANSMIT=1
    PURGE_LINE
    G1 Z20 F3000                   ; move nozzle away from bed
   
[gcode_macro PRINT_END]
gcode:
    M400                           ; wait for buffer to clear
    G92 E0                         ; zero the extruder
    G1 E-10.0 F3600                 ; retract filament
    G92 E0                         ; zero the extruder
    G91                            ; relative positioning
    SET_LED LED="nozzle_light" RED=0.0 GREEN=0.0 BLUE=0.0 SYNC=0 TRANSMIT=1

    #   Get Boundaries
    {% set max_x = printer.configfile.config["stepper_x"]["position_max"]|float %}
    {% set max_y = printer.configfile.config["stepper_y"]["position_max"]|float %}
    {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}

    #   Check end position to determine safe direction to move
    {% if printer.toolhead.position.x < (max_x - 20) %}
        {% set x_safe = 20.0 %}
    {% else %}
        {% set x_safe = -20.0 %}
    {% endif %}

    {% if printer.toolhead.position.y < (max_y - 20) %}
        {% set y_safe = 20.0 %}
    {% else %}
        {% set y_safe = -20.0 %}
    {% endif %}

    {% if printer.toolhead.position.z < (max_z - 2) %}
        {% set z_safe = 2.0 %}
    {% else %}
        {% set z_safe = max_z - printer.toolhead.position.z %}
    {% endif %}

    G0 Z{z_safe} F3600             ; move nozzle up
    G0 X{x_safe} Y{y_safe} F20000  ; move nozzle to remove stringing
    TURN_OFF_HEATERS
    M107                           ; turn off fan
    G90                            ; absolute positioning
    G0 X60 Y{max_y-10} F3600          ; park nozzle at rear
    
[gcode_macro LOAD_FILAMENT]
gcode:
   C_TEMPERATURE_WAIT
   M83                            ; set extruder to relative
   G1 E50 F300                    ; load
   G1 E15 F150                    ; prime nozzle with filament
   M82                            ; set extruder to absolute
    
[gcode_macro UNLOAD_FILAMENT]
gcode:
   C_TEMPERATURE_WAIT
   M83                            ; set extruder to relative
   G1 E10 F300                    ; extrude a little to soften tip
   G1 E-100 F1800                  ; retract some, but not too much or it will jam
   M82                            ; set extruder to absolute


[gcode_macro PURGE_LINE]
gcode:
    {% set y_purge_start = 4 %}
    {% set x_purge_start = 30 %}
    {% set x_purge_end = 90 %} 
    
    C_G28
    C_TEMPERATURE_WAIT
    
    # Move to starting position
    G1 X{x_purge_start} Y{y_purge_start} F5000
    M83                                           ; Set extruder to relative mode
    G0 Z0.3                                       ; Set nozzle height
    M400                                          ; Wait for moves to complete
    
    # First purge line (left to right)
    G1 X{x_purge_start} E18 F1000                 ; Prime the nozzle
    G1 X{x_purge_end} E25 F1000                   ; Draw first line
    
    # Move to second line position
    G0 Y{y_purge_start + 0.4} F5000               ; Move 0.4mm over
    
    # Second purge line (right to left)
    G1 X{x_purge_start} E25 F1000                 ; Draw second line back
    
    # Lift and retract
    G1 E-0.5 F1800                                ; Small retract
    G1 Z10 F3000                                  ; Lift nozzle
    
[gcode_macro PARK_CENTER]
gcode:
    C_G28
    # Get Boundaries
    {% set max_x = printer.configfile.config["stepper_x"]["position_max"]|float %}
    {% set max_y = printer.configfile.config["stepper_y"]["position_max"]|float %}
    {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}
    
    # Calculate center positions
    {% set center_x = max_x / 2 %}
    {% set center_y = max_y / 2 %}
    {% set center_z = max_z / 2 %}
    
    # Use center Z or current Z, whichever is higher (to avoid collision)
    {% set park_z = [printer.toolhead.position.z + 10, center_z]|max %}
    
    # Save current state
    SAVE_GCODE_STATE NAME=PARK_CENTER_STATE
    
    # Move to center
    G90                               # Absolute positioning
    G0 Z{park_z} F3000               # Move to center Z or stay high
    G0 X{center_x} Y{center_y} F6000 # Move to center XY
    
    # Restore state
    RESTORE_GCODE_STATE NAME=PARK_CENTER_STATE